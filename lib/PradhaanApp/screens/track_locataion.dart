/*

import 'dart:ui';

import 'package:flutter/material.dart';

class TrackPage extends StatefulWidget {
   TrackPage({Key? key}) : super(key: key);

  @override
  State<TrackPage> createState() => _TrackPageState();
}

class _TrackPageState extends State<TrackPage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Center(
          child: Row(
            mainAxisAlignment: MainAxisAlignment.spaceBetween,
            children: [
              Image.asset('assets/icon.png',width: 30),
              Text('Village Complaint App'),
              Image.asset('assets/icon.png',width: 30),
            ],
          ),
        ),
      ),

      body: SingleChildScrollView(
        child: Padding(
          padding:  EdgeInsets.all(20.0),
          child: Column(
            mainAxisAlignment: MainAxisAlignment.start,
            children: [
               Text('Track Complaints',style: TextStyle(
                 fontWeight: FontWeight.bold,

                 fontSize: 20,
               ),),
              Text(('You have not submitted any complaints yet, Clicak the submit complaints tab below to do so')),
            ],
          ),
        ),
      ),
    );
  }
}
*/
